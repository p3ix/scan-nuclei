id: spring-actuator-busrefresh-write-surface-potential

info:
  name: Spring Actuator Bus Refresh Write Surface Potentially Exposed
  author: scan-nuclei
  severity: medium
  description: |
    Detects a likely exposed Spring Cloud Bus refresh actuator write surface
    without invoking the operation. A 405 response with POST allowed indicates
    the endpoint exists and should be disabled or strongly authenticated.
  tags:
    - misconfig
    - spring
    - spring-cloud
    - actuator
    - busrefresh
    - write-surface
    - potential

http:
  - method: GET
    path:
      - "{{BaseURL}}/actuator/busrefresh"
      - "{{BaseURL}}/busrefresh"

    matchers-condition: and
    matchers:
      - type: status
        status:
          - 405
      - type: regex
        part: header
        regex:
          - "(?i)allow:\\s*(?:[^\\r\\n,]+,\\s*)*POST\\b"
      - type: regex
        part: body
        condition: or
        regex:
          - "(?i)request method '?GET'? not supported"
          - '(?i)"method"\s*:\s*"GET"'
          - '(?i)"path"\s*:\s*".*/busrefresh"'

    extractors:
      - type: dsl
        dsl:
          - path
        name: exposed_path
