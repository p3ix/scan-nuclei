id: messaging-admin-surface-workflow

info:
  name: Messaging Admin Surface Workflow
  author: scan-nuclei
  description: Runs exposed messaging and broker management checks.

workflows:
  - template: templates/misconfiguration/messaging/rabbitmq-management-exposed.yaml
  - template: templates/exposures/sensitive-paths/java-messaging-config-exposed.yaml
