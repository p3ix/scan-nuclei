id: wildfly-elytron-properties-exposed

info:
  name: WildFly Elytron Properties Exposed
  author: scan-nuclei
  severity: high
  description: |
    Detects exposed Elytron property files that may reveal realm users, groups,
    filesystem paths, or security-domain related configuration.
  tags:
    - exposure
    - wildfly
    - jboss
    - elytron
    - properties
    - security
    - sensitive

http:
  - method: GET
    path:
      - "{{BaseURL}}/application-users.properties"
      - "{{BaseURL}}/application-roles.properties"
      - "{{BaseURL}}/elytron-users.properties"
      - "{{BaseURL}}/elytron-roles.properties"
      - "{{BaseURL}}/config/application-users.properties"
      - "{{BaseURL}}/config/application-roles.properties"
      - "{{BaseURL}}/standalone/configuration/application-users.properties"
      - "{{BaseURL}}/standalone/configuration/application-roles.properties"
      - "{{BaseURL}}/standalone/configuration/elytron-users.properties"
      - "{{BaseURL}}/standalone/configuration/elytron-roles.properties"
      - "{{BaseURL}}/domain/configuration/application-users.properties"
      - "{{BaseURL}}/domain/configuration/application-roles.properties"
      - "{{BaseURL}}/domain/configuration/elytron-users.properties"
      - "{{BaseURL}}/domain/configuration/elytron-roles.properties"

    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
      - type: regex
        part: body
        condition: or
        regex:
          - "(?im)^#\\$REALM_NAME="
          - "(?im)^#\\$ALGORITHM="
          - "(?im)^[A-Za-z0-9_.-]+=([A-Fa-f0-9]{16,}|[A-Za-z0-9_, -]{3,})$"
          - "(?im)^(SuperUser|Administrator|Maintainer|Deployer)\\b"
          - "(?im)^(management|monitor|operator|auditor|admin)[A-Za-z0-9_.-]*="
          - "(?im)^[A-Za-z0-9_.-]+=([A-Za-z0-9_.-]+,){1,}[A-Za-z0-9_.-]+$"

    extractors:
      - type: dsl
        dsl:
          - path
        name: exposed_path
